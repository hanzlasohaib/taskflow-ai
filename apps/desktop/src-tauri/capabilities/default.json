{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the TaskFlow desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
